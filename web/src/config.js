const CONFIG = {
    API_BASE_URL: "http://localhost:3000", // L'URL de votre API JSON Server
    ENDPOINTS: {
        links: "/links",
        favorites: "/favorites",
    }
};

export default CONFIG;